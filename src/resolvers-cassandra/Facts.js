'use strict';

module.exports = {
    // ------------------------------------------------------------------------------------ queries

  list(args, res) { // eslint-disable-line no-unused-vars
  },

  get(args, res) { // eslint-disable-line no-unused-vars
  }
};